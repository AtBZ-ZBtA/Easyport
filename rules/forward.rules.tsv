# Easyport translation rules — Forge 1.20.1 -> NeoForge 1.21.1
#
# Lines are  kind <TAB> from <TAB> to  (to omitted for REMOVED). Blank lines and # ignored.
#
# Deliberately small. Under shim-first, most loader-API references need no rewriting at all:
# forge-compat ships a real net.minecraftforge.* tree, so a mod's existing references resolve
# against it untouched. Rules are only needed where a shim is impossible — which in practice
# means net.minecraft.*, since that package is owned by the game and cannot be shadowed.
#
# SRG -> official member renaming is not expressed here. It is 64,225 entries and lives in
# mappings/srg2official.tsv, applied automatically before any rule below is matched. Rules
# are therefore written against *official* names.
#
# Rule kinds
#   CTOR_TO_STATIC   a constructor became a static factory. Rewrites NEW/DUP/INVOKESPECIAL
#                    into a single INVOKESTATIC. Real bytecode surgery, not a rename.
#   RENAME_METHOD    same shape, different owner or name. A plain call-site rewrite.
#   REMOVED          no replacement exists. Reported, never silently rewritten -- inventing a
#                    target here is exactly the failure mode measured in handport/ (5 false
#                    positives out of 26 symbols).

# --- Loader-scanned annotations: the hard exception to shim-first -------------------------
#
# These MUST be rewritten and cannot be shimmed. FML discovers mods by scanning class files
# for the exact annotation descriptor `Lnet/neoforged/fml/common/Mod;`. A shimmed
# net.minecraftforge.fml.common.Mod in forge-compat is a *different type* as far as that scan
# is concerned, so the mod is never found and never constructed -- it loads, registers
# nothing, and reports no error, which is the worst possible failure mode.
#
# Measured: additional_lights translated cleanly, loaded, and contributed 0 of 326 entries
# purely because its @Mod annotation still said net.minecraftforge.
#
# The rule of thumb: anything the *loader itself* looks up by name must be rewritten.
# Everything a mod merely calls can be shimmed.
TYPE_RENAME	net/minecraftforge/fml/common/Mod	net/neoforged/fml/common/Mod
TYPE_RENAME	net/minecraftforge/fml/common/Mod$EventBusSubscriber	net/neoforged/fml/common/EventBusSubscriber
TYPE_RENAME	net/minecraftforge/fml/common/Mod$EventBusSubscriber$Bus	net/neoforged/fml/common/EventBusSubscriber$Bus
TYPE_RENAME	net/minecraftforge/eventbus/api/SubscribeEvent	net/neoforged/bus/api/SubscribeEvent
TYPE_RENAME	net/minecraftforge/api/distmarker/Dist	net/neoforged/api/distmarker/Dist
TYPE_RENAME	net/minecraftforge/api/distmarker/OnlyIn	net/neoforged/api/distmarker/OnlyIn

# --- Event types: dispatched by identity, so also unshimmable ---------------------------
#
# The event bus resolves a listener's target from the declared parameter type and dispatches
# by exact class. A shimmed FMLCommonSetupEvent would be a distinct class from the one the bus
# actually posts, so the listener would register successfully and then simply never fire --
# again silent, again the worst kind of failure.
#
# Same rule as the annotations above: the loader dispatches these, so they must be rewritten.
# A mod only *calls* ForgeConfigSpec, so that one can stay shimmed.
TYPE_RENAME	net/minecraftforge/eventbus/api/Event	net/neoforged/bus/api/Event
TYPE_RENAME	net/minecraftforge/eventbus/api/EventPriority	net/neoforged/bus/api/EventPriority
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/FMLCommonSetupEvent	net/neoforged/fml/event/lifecycle/FMLCommonSetupEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/FMLClientSetupEvent	net/neoforged/fml/event/lifecycle/FMLClientSetupEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/FMLDedicatedServerSetupEvent	net/neoforged/fml/event/lifecycle/FMLDedicatedServerSetupEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/FMLLoadCompleteEvent	net/neoforged/fml/event/lifecycle/FMLLoadCompleteEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/FMLConstructModEvent	net/neoforged/fml/event/lifecycle/FMLConstructModEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/InterModEnqueueEvent	net/neoforged/fml/event/lifecycle/InterModEnqueueEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/InterModProcessEvent	net/neoforged/fml/event/lifecycle/InterModProcessEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/ModLifecycleEvent	net/neoforged/fml/event/lifecycle/ModLifecycleEvent
TYPE_RENAME	net/minecraftforge/fml/event/lifecycle/ParallelDispatchEvent	net/neoforged/fml/event/lifecycle/ParallelDispatchEvent

# --- ResourceLocation: constructor privatised in 1.21 -------------------------------------
# The single largest vanilla dependency in the corpus: 237 + 188 mods.
CTOR_TO_STATIC	net/minecraft/resources/ResourceLocation	(Ljava/lang/String;Ljava/lang/String;)V	fromNamespaceAndPath	(Ljava/lang/String;Ljava/lang/String;)Lnet/minecraft/resources/ResourceLocation;
CTOR_TO_STATIC	net/minecraft/resources/ResourceLocation	(Ljava/lang/String;)V	parse	(Ljava/lang/String;)Lnet/minecraft/resources/ResourceLocation;

# --- ItemStack NBT -> data components -----------------------------------------------------
# 135 / 111 / 105 / 97 mods. These are NOT renames: the whole idiom changed, and a call-site
# rewrite cannot express getOrCreateTag() returning a mutable tag backed by the stack.
# Recorded as REMOVED so the transformer reports them instead of guessing. The vanilla bridge
# (Phase 4) is what actually resolves these.
REMOVED	net/minecraft/world/item/ItemStack#getOrCreateTag()Lnet/minecraft/nbt/CompoundTag;
REMOVED	net/minecraft/world/item/ItemStack#getTag()Lnet/minecraft/nbt/CompoundTag;
REMOVED	net/minecraft/world/item/ItemStack#setTag(Lnet/minecraft/nbt/CompoundTag;)V
REMOVED	net/minecraft/world/item/ItemStack#hasTag()Z

# --- Formerly listed as removed; both are now shimmed ------------------------------------
# FMLJavaModLoadingContext#get and ModLoadingContext#get were recorded here as having no
# replacement, on the reasoning that NeoForge injects the bus into the mod constructor. That
# was wrong: NeoForge kept ModLoadingContext.get() and exposes the bus through
# getActiveContainer().getEventBus(), so both are plain delegating shims in forge-compat.
#
# Left as a note rather than deleted, because a stale REMOVED entry is actively harmful --
# every translation report flagged a resolved symbol as unresolvable, which sends whoever
# reads the report chasing work that is already done.

# --- Vanilla constructors whose arguments were reordered ---------------------------------
#
# CTOR_SWAP2  owner <TAB> oldDesc <TAB> newDesc <TAB> [narrowTopTo]
#
# TorchBlock went from (Properties, ParticleOptions) to (SimpleParticleType, Properties) in
# 1.21 -- reordered and narrowed at once. The narrowing needs a CHECKCAST emitted before the
# swap, while the value is still reachable on top of the stack.
CTOR_SWAP2	net/minecraft/world/level/block/TorchBlock	(Lnet/minecraft/world/level/block/state/BlockBehaviour$Properties;Lnet/minecraft/core/particles/ParticleOptions;)V	(Lnet/minecraft/core/particles/SimpleParticleType;Lnet/minecraft/world/level/block/state/BlockBehaviour$Properties;)V	net/minecraft/core/particles/SimpleParticleType
CTOR_SWAP2	net/minecraft/world/level/block/WallTorchBlock	(Lnet/minecraft/world/level/block/state/BlockBehaviour$Properties;Lnet/minecraft/core/particles/ParticleOptions;)V	(Lnet/minecraft/core/particles/SimpleParticleType;Lnet/minecraft/world/level/block/state/BlockBehaviour$Properties;)V	net/minecraft/core/particles/SimpleParticleType

# --- Plain method renames ------------------------------------------------------------------
#
# RENAME_METHOD  owner <TAB> name <TAB> desc <TAB> newOwner <TAB> newName <TAB> newDesc
#
# The kind a rename table handles well -- and the kind corpus mining scored 13 out of 13 on in
# the hand-port. Kept explicit rather than auto-imported from candidate-rules-strong.tsv so
# every entry has been checked; the mining is a source of leads, not of truth.
RENAME_METHOD	net/minecraft/world/item/Item$Properties	defaultDurability	(I)Lnet/minecraft/world/item/Item$Properties;	net/minecraft/world/item/Item$Properties	durability	(I)Lnet/minecraft/world/item/Item$Properties;

# --- Event packages: scoped prefix renames ------------------------------------------------
#
# TYPE_PREFIX_RENAME  fromPrefix <TAB> toPrefix
#
# A prefix rename is normally the wrong tool -- a blanket net.minecraftforge -> net.neoforged
# rename would defeat the shim layer entirely. It is correct here precisely because these
# packages contain nothing but event classes, and events are dispatched by exact class
# identity, so every one of them must be rewritten. Enumerating several hundred individually
# would be noise, and any that got missed would fail silently by simply never firing.
#
# Exact TYPE_RENAME entries above still win: they are checked first.
# TickEvent is bridged in forge-compat rather than renamed: NeoForge split one phase-carrying
# class into Pre/Post, and renaming ClientTickEvent to ClientTickEvent$Post would silently
# misfire every START-phase listener. Exact renames are checked before prefixes, so an explicit
# no-op entry keeps the prefix rule from touching it.
TYPE_RENAME	net/minecraftforge/event/TickEvent	net/minecraftforge/event/TickEvent
TYPE_RENAME	net/minecraftforge/event/TickEvent$Phase	net/minecraftforge/event/TickEvent$Phase
TYPE_RENAME	net/minecraftforge/event/TickEvent$ClientTickEvent	net/minecraftforge/event/TickEvent$ClientTickEvent
TYPE_RENAME	net/minecraftforge/event/TickEvent$ServerTickEvent	net/minecraftforge/event/TickEvent$ServerTickEvent
TYPE_RENAME	net/minecraftforge/event/TickEvent$LevelTickEvent	net/minecraftforge/event/TickEvent$LevelTickEvent
TYPE_RENAME	net/minecraftforge/event/TickEvent$PlayerTickEvent	net/minecraftforge/event/TickEvent$PlayerTickEvent
TYPE_PREFIX_RENAME	net/minecraftforge/event/	net/neoforged/neoforge/event/
TYPE_PREFIX_RENAME	net/minecraftforge/client/event/	net/neoforged/neoforge/client/event/
TYPE_PREFIX_RENAME	net/minecraftforge/data/event/	net/neoforged/neoforge/data/event/

# --- fml event packages (config + lifecycle) ---------------------------------------------
# Supersedes the nine individual FML lifecycle TYPE_RENAME entries above: net/neoforged/fml/
# event/ covers both the lifecycle and config trees, so one rule replaces them and there are
# eight fewer places for the list to drift out of date. Also picks up ModConfigEvent, which
# the hand-written list had simply missed.
TYPE_PREFIX_RENAME	net/minecraftforge/fml/event/	net/neoforged/fml/event/

# --- Removed vanilla types: relocate, then rename -----------------------------------------
#
# 1.21 deleted these outright. Supplying the class in its original package is impossible --
# net.minecraft is owned by the game's module and a second jar exporting into it fails module
# resolution (proven in testkit/vanilla-package-probe). So forge-compat carries a stand-in
# under easyport.vanilla and references are rewritten to it.
#
# Restores loading, not behaviour: vanilla drives loot serialization through codecs now and
# will never call the shim's methods, so custom loot conditions and functions stay inert. The
# rest of the mod works. Generating a codec from the mod's serialize/deserialize pair is
# Phase 4 vanilla-bridge work.
TYPE_RENAME	net/minecraft/world/level/storage/loot/Serializer	easyport/vanilla/LootSerializer

# --- More reordered vanilla constructors ---------------------------------------------------
# Same shape as TorchBlock: 1.21 moved the Properties argument last. No narrowing needed here,
# both arguments are already the right types.
CTOR_SWAP2	net/minecraft/world/level/block/DropExperienceBlock	(Lnet/minecraft/world/level/block/state/BlockBehaviour$Properties;Lnet/minecraft/util/valueproviders/IntProvider;)V	(Lnet/minecraft/util/valueproviders/IntProvider;Lnet/minecraft/world/level/block/state/BlockBehaviour$Properties;)V

# --- Loader utilities that map 1:1 --------------------------------------------------------
#
# Shim-first exists because most Forge APIs do not map cleanly onto NeoForge. These do: same
# class, same name, different namespace. Renaming is cheaper than writing and maintaining a
# shim that would only forward every call verbatim.
#
# The risk is that a signature differs somewhere inside one of them, which shows up as
# NoSuchMethodError rather than ClassNotFoundException. If that happens, convert that specific
# entry to a shim -- a shim can adapt a signature where a rename cannot.
#
# These block the eight highest-fan-in library mods in the corpus, which together have ~70
# dependents. Fixing a library is worth many tail mods.
TYPE_RENAME	net/minecraftforge/fml/loading/FMLLoader	net/neoforged/fml/loading/FMLLoader
# ModLoader removed from this list: renaming resolved the class and then failed on
# isDataGenRunning(), which NeoForge does not have. It is a shim in forge-compat instead.
TYPE_RENAME	net/minecraftforge/common/crafting/CraftingHelper	net/neoforged/neoforge/common/crafting/CraftingHelper
TYPE_RENAME	net/minecraftforge/data/loading/DatagenModLoader	net/neoforged/neoforge/data/loading/DatagenModLoader
TYPE_RENAME	net/minecraftforge/registries/GameData	net/neoforged/neoforge/registries/GameData

# RegisterEvent is dispatched by identity like every other event, so it must be renamed
# regardless of whether a shim would have been possible.
TYPE_RENAME	net/minecraftforge/registries/RegisterEvent	net/neoforged/neoforge/registries/RegisterEvent
TYPE_RENAME	net/minecraftforge/registries/RegisterEvent$RegisterHelper	net/neoforged/neoforge/registries/RegisterEvent$RegisterHelper

# NewRegistryEvent survived the fork under the same name in NeoForge's package, and NeoForge
# still posts it on the mod bus -- so unlike TickEvent this needs no bridge, only a rename.
# 22 corpus jars reference it, including architectury and cyclopscore.
#
# Known sharp edge: the two versions agree on the class but not on all of its methods. Forge's
# create(RegistryBuilder) returned Supplier<IForgeRegistry<V>>; NeoForge's returns Registry<T>
# and takes its own RegistryBuilder. Mods that only *receive* the event -- cyclopscore ignores
# its argument entirely -- are fine. Mods that call create() will surface a NoSuchMethodError,
# which is loud, and the fix at that point is to convert this entry to a shim that adapts the
# signature. Renaming first because it costs one line and covers the common case.
TYPE_RENAME	net/minecraftforge/registries/NewRegistryEvent	net/neoforged/neoforge/registries/NewRegistryEvent

# ModContainer moved package but kept its shape. cyclopscore names it in six classes.
TYPE_RENAME	net/minecraftforge/fml/ModContainer	net/neoforged/fml/ModContainer

# TextureStitchEvent.Post became TextureAtlasStitchedEvent -- a rename the scoped prefix rule for
# net/minecraftforge/client/event/ cannot express, since the simple name changed too. The prefix
# rule produced neoforge/client/event/TextureStitchEvent$Post, which does not exist, and the
# target-existence check correctly refused it -- leaving the original name to fail at load.
#
# Pre has no counterpart: NeoForge removed pre-stitch sprite injection entirely. Left unmapped
# deliberately, so a mod that uses it fails loudly rather than binding to the wrong event.
# 16 corpus jars reference this family; architectury's listener is an empty method.
TYPE_RENAME	net/minecraftforge/client/event/TextureStitchEvent$Post	net/neoforged/neoforge/client/event/TextureAtlasStitchedEvent

# Biome modification survived the fork nearly intact -- same names, NeoForge's package.
# 37 corpus jars.
#
# One known incompatibility, deliberately accepted: Forge's BiomeModifier.codec() returns
# Codec<? extends BiomeModifier>, NeoForge's returns MapCodec. A renamed implementation still
# declares the Codec-returning method, so it no longer overrides anything and calling codec()
# through the interface throws AbstractMethodError. That is loud and late rather than silent,
# and the mod loads -- where without the rename it does not load at all.
#
# The underlying cause is the 1.20.5 Codec/MapCodec split, which affects far more than biomes
# and needs its own pass. Recorded in ROADMAP rather than worked around here.
TYPE_RENAME	net/minecraftforge/common/world/BiomeModifier	net/neoforged/neoforge/common/world/BiomeModifier
TYPE_RENAME	net/minecraftforge/common/world/BiomeModifier$Phase	net/neoforged/neoforge/common/world/BiomeModifier$Phase
TYPE_RENAME	net/minecraftforge/common/world/ModifiableBiomeInfo	net/neoforged/neoforge/common/world/ModifiableBiomeInfo
TYPE_RENAME	net/minecraftforge/common/world/ModifiableBiomeInfo$BiomeInfo	net/neoforged/neoforge/common/world/ModifiableBiomeInfo$BiomeInfo
TYPE_RENAME	net/minecraftforge/common/world/ModifiableBiomeInfo$BiomeInfo$Builder	net/neoforged/neoforge/common/world/ModifiableBiomeInfo$BiomeInfo$Builder

# ---------------------------------------------------------------------------------------------
# Wholesale package moves.
#
# Derived from api-report/unresolved-types.txt, which lists every Forge type the corpus actually
# references that nothing currently resolves, alongside any platform class with the same name.
# NeoForge reorganised namespaces far more than it renamed things, so most of the list is one
# package move repeated -- these rules are that observation written down.
#
# Broad prefixes are safe here specifically because Translate refuses a rename whose target does
# not exist in the platform index (see renameTargetExists). A prefix that overreaches produces a
# missing target and is dropped, leaving the original name to be resolved by a shim or to fail
# loudly. That property is what makes it reasonable to map a whole package rather than enumerate
# it -- and it is why these must never be added without the platform jars indexed.
#
# Counts are jars in the 433-jar ATM9 corpus referencing the busiest type in each package.
TYPE_PREFIX_RENAME	net/minecraftforge/items/	net/neoforged/neoforge/items/
TYPE_PREFIX_RENAME	net/minecraftforge/fluids/	net/neoforged/neoforge/fluids/
TYPE_PREFIX_RENAME	net/minecraftforge/energy/	net/neoforged/neoforge/energy/
TYPE_PREFIX_RENAME	net/minecraftforge/client/model/	net/neoforged/neoforge/client/model/
TYPE_PREFIX_RENAME	net/minecraftforge/client/settings/	net/neoforged/neoforge/client/settings/
TYPE_PREFIX_RENAME	net/minecraftforge/client/extensions/	net/neoforged/neoforge/client/extensions/
TYPE_PREFIX_RENAME	net/minecraftforge/common/loot/	net/neoforged/neoforge/common/loot/
TYPE_PREFIX_RENAME	net/minecraftforge/common/data/	net/neoforged/neoforge/common/data/
TYPE_PREFIX_RENAME	net/minecraftforge/server/	net/neoforged/neoforge/server/

# Tags is a single class whose nested tag holders (Tags$Items, Tags$Blocks) are what mods use.
# Written as a prefix so the nested types come along without enumerating them.
TYPE_PREFIX_RENAME	net/minecraftforge/common/Tags	net/neoforged/neoforge/common/Tags

# Renamed packages, where the target path is not a mechanical substitution of the source.
TYPE_PREFIX_RENAME	net/minecraftforge/forgespi/	net/neoforged/neoforgespi/
TYPE_PREFIX_RENAME	net/minecraftforge/fml/util/	net/neoforged/fml/util/
# The conditions package moved, and this was a prefix rule until the shadowed-shim check caught
# what that did to ICondition: 1.20.5 added a second abstract method, codec(), which no mod
# compiled against 1.20.1 can have implemented. The rename resolved cleanly and left every custom
# condition one AbstractMethodError away from failing the first time a recipe used it.
#
# Enumerated rather than excluded, because a prefix rule cannot say "all of these but one" and
# the alternatives all rely on rule-precedence subtleties that would not survive being read later.
# ICondition and IConditionSerializer stay shimmed; everything else here is shape-identical in
# NeoForge and renames. Counts are corpus jars.
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/ModLoadedCondition	net/neoforged/neoforge/common/conditions/ModLoadedCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/IConditionBuilder	net/neoforged/neoforge/common/conditions/IConditionBuilder
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/NotCondition	net/neoforged/neoforge/common/conditions/NotCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/TagEmptyCondition	net/neoforged/neoforge/common/conditions/TagEmptyCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/OrCondition	net/neoforged/neoforge/common/conditions/OrCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/AndCondition	net/neoforged/neoforge/common/conditions/AndCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/TrueCondition	net/neoforged/neoforge/common/conditions/TrueCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/FalseCondition	net/neoforged/neoforge/common/conditions/FalseCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/ItemExistsCondition	net/neoforged/neoforge/common/conditions/ItemExistsCondition
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/ConditionContext	net/neoforged/neoforge/common/conditions/ConditionContext

# Capabilities: deliberately NOT renamed. This rule existed briefly and was wrong.
#
# The names survived the move, so a prefix rule looks like it works -- ICapabilityProvider and
# RegisterCapabilitiesEvent both exist under net/neoforged/neoforge/capabilities/. The shapes did
# not survive:
#
#   Forge     ICapabilityProvider          { LazyOptional<T> getCapability(Capability<T>, Direction) }
#   NeoForge  ICapabilityProvider<O, C, T> { T getCapability(O, C) }
#
# 106 corpus jars *implement* Forge's interface. Renaming makes each of them declare a method
# that overrides nothing and leaves NeoForge's abstract method unimplemented -- the class still
# loads, and fails with AbstractMethodError whenever something asks it for a capability, which
# may be deep into a game. RegisterCapabilitiesEvent is the same story: it exists, and its
# register(Class) -- called by 68 jars -- does not.
#
# Both were caught by the member check in tools/RenameGaps.java rather than by a launch, which
# is the point of that check. Left unresolved and visible in the gap report until the capability
# bridge is designed; a rename that resolves onto an incompatible type is worse than no rule,
# because the gap report then stops mentioning it.

# --- Restructured events: renamed, not merely moved -------------------------------------------
#
# READ THIS BEFORE TRUSTING THE BLOCK BELOW.
#
# These renames are load-enabling stopgaps, not correct translations. NeoForge renamed the
# events' *accessors* along with the events, and a type rename cannot follow that:
#
#   LivingHurtEvent.getAmount()        -> LivingDamageEvent$Pre has getNewDamage()
#   EntityItemPickupEvent.getEntity()  -> ItemEntityPickupEvent$Pre has getPlayer()
#   SpawnPlacementRegisterEvent.register(...) -> different signature entirely
#
# So a renamed listener resolves its event, registers, fires -- and throws NoSuchMethodError on
# the first accessor it touches. Every instance is listed by the member check in RenameGaps.
#
# Kept anyway, deliberately: without them the mod does not load at all and contributes nothing,
# whereas with them it loads, registers its content, and fails only if that specific event
# actually fires. Loud and narrow beats total and silent. But they are a stepping stone.
#
# The real fix is the shim-and-bridge shape already used for TickEvent: a forge-compat class
# carrying the Forge accessor names, plus a bridge subscribing to the NeoForge event and
# re-posting the Forge-shaped one. That is the plan for the whole restructured-event family and
# it is not done.
#
# The prefix rules above handle events NeoForge only repackaged. These are the ones it also
# renamed or split, where a prefix rule either produces a missing target (harmless -- refused and
# reported) or, far worse, lands on a real class that is never posted.
#
# Damage events were reordered and renamed together. NeoForge's own migration guidance gives the
# mapping, which is not guessable from the names:
#   LivingAttackEvent  fires before any reduction        -> LivingIncomingDamageEvent
#   LivingHurtEvent    fires after reduction, before use -> LivingDamageEvent$Pre
#   LivingDamageEvent  fires after damage is applied     -> LivingDamageEvent$Post
#
# The third is the dangerous one and the reason tools/RenameGaps.java now flags abstract targets.
# Forge's LivingDamageEvent is concrete and posted; NeoForge kept the name as an *abstract* parent
# for its Pre/Post pair. The prefix rule resolved it happily, and 24 jars' listeners would have
# registered against a class nothing ever posts -- loading cleanly and silently doing nothing.
TYPE_RENAME	net/minecraftforge/event/entity/living/LivingAttackEvent	net/neoforged/neoforge/event/entity/living/LivingIncomingDamageEvent
TYPE_RENAME	net/minecraftforge/event/entity/living/LivingHurtEvent	net/neoforged/neoforge/event/entity/living/LivingDamageEvent$Pre
TYPE_RENAME	net/minecraftforge/event/entity/living/LivingDamageEvent	net/neoforged/neoforge/event/entity/living/LivingDamageEvent$Post

# Ticking moved out of the per-entity event and into the shared tick package. Forge's
# LivingTickEvent had no phase field -- it fired once, cancellably, before the tick -- so Pre is
# an exact match rather than the coin-flip that Pre/Post mapping usually is.
# Withdrawn: LivingTickEvent -> EntityTickEvent$Pre.
#
# The event does correspond, but Forge's getEntity() returns LivingEntity and NeoForge's returns
# Entity, so all 41 jars calling it would take a NoSuchMethodError -- a rename cannot narrow a
# return type. Needs a shim that wraps the NeoForge event and re-declares the accessor, which is
# the same bridging shape as TickEvent. Flagged by the member check, not by a launch.
# TYPE_RENAME	net/minecraftforge/event/entity/living/LivingEvent$LivingTickEvent	net/neoforged/neoforge/event/tick/EntityTickEvent$Pre

# Pickup split into a cancellable Pre and an informational Post. Forge had one of each already
# under different names, so both map cleanly.
TYPE_RENAME	net/minecraftforge/event/entity/player/EntityItemPickupEvent	net/neoforged/neoforge/event/entity/player/ItemEntityPickupEvent$Pre
TYPE_RENAME	net/minecraftforge/event/entity/player/PlayerEvent$ItemPickupEvent	net/neoforged/neoforge/event/entity/player/ItemEntityPickupEvent$Post

# Straight renames.
TYPE_RENAME	net/minecraftforge/event/entity/living/MobSpawnEvent$FinalizeSpawn	net/neoforged/neoforge/event/entity/living/FinalizeSpawnEvent
TYPE_RENAME	net/minecraftforge/event/entity/living/ShieldBlockEvent	net/neoforged/neoforge/event/entity/living/LivingShieldBlockEvent
TYPE_RENAME	net/minecraftforge/event/entity/SpawnPlacementRegisterEvent	net/neoforged/neoforge/event/entity/RegisterSpawnPlacementsEvent
TYPE_RENAME	net/minecraftforge/event/entity/SpawnPlacementRegisterEvent$Operation	net/neoforged/neoforge/event/entity/RegisterSpawnPlacementsEvent$Operation
TYPE_RENAME	net/minecraftforge/event/level/SaplingGrowTreeEvent	net/neoforged/neoforge/event/level/BlockGrowFeatureEvent
TYPE_RENAME	net/minecraftforge/event/level/BlockEvent$CropGrowEvent$Pre	net/neoforged/neoforge/event/level/block/CropGrowEvent$Pre
TYPE_RENAME	net/minecraftforge/event/level/BlockEvent$CropGrowEvent$Post	net/neoforged/neoforge/event/level/block/CropGrowEvent$Post
TYPE_RENAME	net/minecraftforge/event/entity/player/PlayerSleepInBedEvent	net/neoforged/neoforge/event/entity/player/CanPlayerSleepEvent

# ForgeEventFactory was split in two. Neither half is a superset, so this needs per-method rules
# rather than a type rename; left unmapped until the methods are mined. 80 jars, so it matters.
#   net/neoforged/neoforge/event/EventHooks     -- posting game events
#   net/neoforged/neoforge/common/CommonHooks   -- everything else
#
# Deliberately unmapped, no replacement exists:
#   AttachCapabilitiesEvent (86)  capabilities are no longer attached to objects
#   FillBucketEvent (6)           replaced by fluid capability interactions
#   LootingLevelEvent (7)         removed by the 1.21 enchantment rework

# --- Instance calls that must become static ---------------------------------------------------
#
# METHOD_TO_STATIC  owner <TAB> name <TAB> desc <TAB> newOwner <TAB> newName <TAB> newDesc
#
# For the case neither a rename nor a shim can reach: a type that MUST be renamed, because the
# loader dispatches on it and a shimmed copy would never fire, whose replacement is missing a
# method the corpus calls. RegisterEvent is the first instance -- placebo calls
# getForgeRegistry() on it, and NeoForge dropped that method.
#
# The rewrite is cheap because the stack already suits it: when INVOKEVIRTUAL runs, the receiver
# sits below the arguments in exactly the slot INVOKESTATIC reads its first parameter from. So
# the owner becomes the first descriptor argument and nothing has to be shuffled.
#
# Owners here are written POST-rename -- these rules run after the type remapper, so the owner
# is already net/neoforged/... even though the mod was compiled against net/minecraftforge/...
#
# This is the shape for the rest of the "rename target missing a called member" list in
# api-report/unresolved-types.txt, which currently runs to several hundred entries.
METHOD_TO_STATIC	net/neoforged/neoforge/registries/RegisterEvent	getForgeRegistry	()Lnet/minecraftforge/registries/IForgeRegistry;	easyport/bridge/RegistryBridge	getForgeRegistry	(Lnet/neoforged/neoforge/registries/RegisterEvent;)Lnet/minecraftforge/registries/IForgeRegistry;

# common/util was a prefix rule and should not have been.
#
# The package is split down the middle. NeoForge kept FakePlayer, Lazy, ITeleporter and friends
# at the same path, so those rename cleanly. It also kept INBTSerializable there -- with
# different method signatures. Its serializeNBT/deserializeNBT gained a HolderLookup.Provider
# parameter in 1.20.5, when NBT started needing registry access, so a Forge implementor
# overrides nothing and leaves the real interface methods abstract. 61 jars implement it.
#
# LazyOptional and the NonNull* functional interfaces have no NeoForge equivalent at all and are
# shimmed; the prefix rule was harmless for them only because the target-existence check refused
# it. Relying on that is relying on an accident.
#
# So: enumerate the ones that genuinely moved, and let everything else fall through to a shim.
TYPE_RENAME	net/minecraftforge/common/util/FakePlayer	net/neoforged/neoforge/common/util/FakePlayer
TYPE_RENAME	net/minecraftforge/common/util/FakePlayerFactory	net/neoforged/neoforge/common/util/FakePlayerFactory
TYPE_RENAME	net/minecraftforge/common/util/Lazy	net/neoforged/neoforge/common/util/Lazy
TYPE_RENAME	net/minecraftforge/common/util/ITeleporter	net/neoforged/neoforge/common/util/ITeleporter
TYPE_RENAME	net/minecraftforge/common/util/BlockSnapshot	net/neoforged/neoforge/common/util/BlockSnapshot
TYPE_RENAME	net/minecraftforge/common/util/MutableHashedLinkedMap	net/neoforged/neoforge/common/util/MutableHashedLinkedMap
TYPE_RENAME	net/minecraftforge/common/util/LogicalSidedProvider	net/neoforged/neoforge/common/util/LogicalSidedProvider

# --- Vanilla constants wrapped in Holder --- WITHDRAWN, now handled automatically -------------
#
# Two rules used to live here and both are gone:
#
#   FIELD_RETYPE  net/minecraft/world/item/ArmorMaterials  Lnet/minecraft/core/Holder;
#   RENAME_METHOD net/minecraft/world/item/ArmorItem  <init>  (ArmorMaterial,Type,Properties)V
#                                                  -> (Holder,Type,Properties)V
#
# They were the right diagnosis and the wrong treatment. 1.21 wrapped a family of vanilla
# constants -- MobEffects, Attributes, Potions, SoundEvents, ArmorMaterials -- and these rules
# let the new Holder type spread outward from the field read. It does not stop at the vanilla
# boundary: geckolib still failed, one layer in, because its *own* WolfArmorItem constructor
# declares ArmorMaterial and the call site now passed a Holder.
#
# Chasing that would mean propagating a retype through mod signatures by data flow. Easyport
# does the opposite instead -- unwraps at every vanilla boundary so the mod's own bytecode keeps
# the types its author compiled against, and wraps again on the way back in. See
# Translate#applyHolderUnwrap and #applyWrapAdapters, which discover the whole family from the
# platform's own descriptors and need no rules here at all.
#
# Left as a comment rather than deleted: re-adding either one silently disables the automatic
# pass for that type, because a field already retyped to Holder no longer looks like a gap.

# ItemHandlerHelper kept most of its class in NeoForge but lost these two, both of which
# vanilla absorbed in 1.20.5 when component comparison replaced NBT comparison. Static to
# static, so a plain call-site rewrite covers them. Owners are post-rename. 47 and 39 jars.
RENAME_METHOD	net/neoforged/neoforge/items/ItemHandlerHelper	canItemStacksStack	(Lnet/minecraft/world/item/ItemStack;Lnet/minecraft/world/item/ItemStack;)Z	easyport/bridge/ItemBridge	canItemStacksStack	(Lnet/minecraft/world/item/ItemStack;Lnet/minecraft/world/item/ItemStack;)Z
RENAME_METHOD	net/neoforged/neoforge/items/ItemHandlerHelper	copyStackWithSize	(Lnet/minecraft/world/item/ItemStack;I)Lnet/minecraft/world/item/ItemStack;	easyport/bridge/ItemBridge	copyStackWithSize	(Lnet/minecraft/world/item/ItemStack;I)Lnet/minecraft/world/item/ItemStack;

# IEventBus.post returned boolean in Forge and returns the event in NeoForge. The shim extends
# NeoForge s interface, so declaring both is a clashing-erasure compile error -- no shim and no
# rename can express a return-type change on an inherited method. The call site moves instead.
# 110 jars. Owner is the Forge name because IEventBus is shimmed, not renamed.
METHOD_TO_STATIC	net/minecraftforge/eventbus/api/IEventBus	post	(Lnet/neoforged/bus/api/Event;)Z	easyport/bridge/EventBusBridge	post	(Lnet/neoforged/bus/api/IEventBus;Lnet/neoforged/bus/api/Event;)Z

# ModConfigEvent is renamed (the mod bus dispatches on it), so its getConfig() returns NeoForge s
# ModConfig where the mod expects Forge s. The class resolves and the call does not. The subclass
# entries are separate because the owner in the bytecode is whichever type the listener declared.
# 34 + 33 + 28 jars.
METHOD_TO_STATIC	net/neoforged/fml/event/config/ModConfigEvent	getConfig	()Lnet/minecraftforge/fml/config/ModConfig;	easyport/bridge/ConfigBridge	getConfig	(Lnet/neoforged/fml/event/config/ModConfigEvent;)Lnet/minecraftforge/fml/config/ModConfig;
METHOD_TO_STATIC	net/neoforged/fml/event/config/ModConfigEvent$Loading	getConfig	()Lnet/minecraftforge/fml/config/ModConfig;	easyport/bridge/ConfigBridge	getConfig	(Lnet/neoforged/fml/event/config/ModConfigEvent;)Lnet/minecraftforge/fml/config/ModConfig;
METHOD_TO_STATIC	net/neoforged/fml/event/config/ModConfigEvent$Reloading	getConfig	()Lnet/minecraftforge/fml/config/ModConfig;	easyport/bridge/ConfigBridge	getConfig	(Lnet/neoforged/fml/event/config/ModConfigEvent;)Lnet/minecraftforge/fml/config/ModConfig;


# --- Loader constants the target platform deleted --------------------------------------------
#
# FIELD_TO_STATIC  owner <TAB> name <TAB> desc <TAB> newOwner <TAB> newName <TAB> newDesc
#
# A GETSTATIC pushes one value and a no-arg INVOKESTATIC pushes one value, so swapping the
# instruction is the whole rewrite -- the same shape as METHOD_TO_STATIC.
#
# cyclopscore reads IEnvironment.Keys.NAMING to detect a dev environment. modlauncher removed the
# field, so the read throws NoSuchFieldError during mod construction and takes down a library ten
# mods depend on. Not a Forge API, but the same class of problem.
FIELD_TO_STATIC	cpw/mods/modlauncher/api/IEnvironment$Keys	NAMING	Ljava/util/function/Supplier;	easyport/bridge/EnvBridge	naming	()Ljava/util/function/Supplier;

# --- Bulk package moves, generated from the gap report ----------------------------------------
#
# Every remaining unresolved type whose simple name matches exactly one platform class. These are
# the tail of the same wholesale namespace reorganisation the prefix rules above cover -- classes
# that sit outside any package that could be moved as a unit.
#
# Generated rather than hand-listed, then left to the two checks that follow: the target-existence
# check in Translate refuses any that do not resolve, and the member check in RenameGaps reports
# any whose shape does not match what the corpus calls. A generated rule that is wrong therefore
# surfaces as a report line rather than as a silent mistranslation.
TYPE_RENAME	net/minecraftforge/fml/InterModComms	net/neoforged/fml/InterModComms
TYPE_RENAME	net/minecraftforge/client/ChunkRenderTypeSet	net/neoforged/neoforge/client/ChunkRenderTypeSet
TYPE_RENAME	net/minecraftforge/common/SoundActions	net/neoforged/neoforge/common/SoundActions
TYPE_RENAME	net/minecraftforge/registries/RegistryBuilder	net/neoforged/neoforge/registries/RegistryBuilder
TYPE_RENAME	net/minecraftforge/fml/loading/LoadingModList	net/neoforged/fml/loading/LoadingModList
TYPE_RENAME	net/minecraftforge/fml/InterModComms$IMCMessage	net/neoforged/fml/InterModComms$IMCMessage
TYPE_RENAME	net/minecraftforge/common/brewing/BrewingRecipeRegistry	net/neoforged/neoforge/common/brewing/BrewingRecipeRegistry
TYPE_RENAME	net/minecraftforge/resource/PathPackResources	net/minecraft/server/packs/PathPackResources
TYPE_RENAME	net/minecraftforge/entity/PartEntity	net/neoforged/neoforge/entity/PartEntity
TYPE_RENAME	net/minecraftforge/common/world/BiomeGenerationSettingsBuilder	net/neoforged/neoforge/common/world/BiomeGenerationSettingsBuilder
TYPE_RENAME	net/minecraftforge/registries/DataPackRegistryEvent$NewRegistry	net/neoforged/neoforge/registries/DataPackRegistryEvent$NewRegistry
TYPE_RENAME	net/minecraftforge/client/RenderTypeGroup	net/neoforged/neoforge/client/RenderTypeGroup
TYPE_RENAME	net/minecraftforge/common/brewing/BrewingRecipe	net/neoforged/neoforge/common/brewing/BrewingRecipe
TYPE_RENAME	net/minecraftforge/fml/loading/moddiscovery/ModInfo	net/neoforged/fml/loading/moddiscovery/ModInfo
TYPE_RENAME	net/minecraftforge/common/brewing/IBrewingRecipe	net/neoforged/neoforge/common/brewing/IBrewingRecipe
TYPE_RENAME	net/minecraftforge/common/crafting/CompoundIngredient	net/neoforged/neoforge/common/crafting/CompoundIngredient
TYPE_RENAME	net/minecraftforge/fml/javafmlmod/FMLModContainer	net/neoforged/fml/javafmlmod/FMLModContainer
TYPE_RENAME	net/minecraftforge/registries/RegistryManager	net/neoforged/neoforge/registries/RegistryManager
TYPE_RENAME	net/minecraftforge/client/gui/widget/ExtendedButton	net/neoforged/neoforge/client/gui/widget/ExtendedButton
TYPE_RENAME	net/minecraftforge/common/UsernameCache	net/neoforged/neoforge/common/UsernameCache
TYPE_RENAME	net/minecraftforge/common/world/MobSpawnSettingsBuilder	net/neoforged/neoforge/common/world/MobSpawnSettingsBuilder
TYPE_RENAME	net/minecraftforge/fml/loading/moddiscovery/ModFileInfo	net/neoforged/fml/loading/moddiscovery/ModFileInfo
TYPE_RENAME	net/minecraftforge/client/RenderTypeHelper	net/neoforged/neoforge/client/RenderTypeHelper
TYPE_RENAME	net/minecraftforge/common/util/RecipeMatcher	net/neoforged/neoforge/common/util/RecipeMatcher
TYPE_RENAME	net/minecraftforge/fml/VersionChecker$Status	net/neoforged/fml/VersionChecker$Status
TYPE_RENAME	net/minecraftforge/fml/loading/targets/CommonLaunchHandler	net/neoforged/fml/loading/targets/CommonLaunchHandler
TYPE_RENAME	net/minecraftforge/client/gui/widget/ScrollPanel	net/neoforged/neoforge/client/gui/widget/ScrollPanel
TYPE_RENAME	net/minecraftforge/common/FarmlandWaterManager	net/neoforged/neoforge/common/FarmlandWaterManager
TYPE_RENAME	net/minecraftforge/common/util/TriPredicate	net/neoforged/neoforge/common/util/TriPredicate
TYPE_RENAME	net/minecraftforge/resource/ResourcePackLoader	net/neoforged/neoforge/resource/ResourcePackLoader
TYPE_RENAME	net/minecraftforge/client/IItemDecorator	net/neoforged/neoforge/client/IItemDecorator
TYPE_RENAME	net/minecraftforge/common/CreativeModeTabRegistry	net/neoforged/neoforge/common/CreativeModeTabRegistry
TYPE_RENAME	net/minecraftforge/common/util/ConcatenatedListView	net/neoforged/neoforge/common/util/ConcatenatedListView
TYPE_RENAME	net/minecraftforge/fml/VersionChecker$CheckResult	net/neoforged/fml/VersionChecker$CheckResult
TYPE_RENAME	net/minecraftforge/fml/loading/FMLConfig	net/neoforged/fml/loading/FMLConfig
TYPE_RENAME	net/minecraftforge/client/ItemDecoratorHandler	net/neoforged/neoforge/client/ItemDecoratorHandler
TYPE_RENAME	net/minecraftforge/common/BasicItemListing	net/neoforged/neoforge/common/BasicItemListing
TYPE_RENAME	net/minecraftforge/fml/VersionChecker	net/neoforged/fml/VersionChecker
TYPE_RENAME	net/minecraftforge/fml/loading/moddiscovery/ModFile	net/neoforged/fml/loading/moddiscovery/ModFile
TYPE_RENAME	net/minecraftforge/registries/holdersets/OrHolderSet	net/neoforged/neoforge/registries/holdersets/OrHolderSet
TYPE_RENAME	net/minecraftforge/common/crafting/DifferenceIngredient	net/neoforged/neoforge/common/crafting/DifferenceIngredient
TYPE_RENAME	net/minecraftforge/common/crafting/IntersectionIngredient	net/neoforged/neoforge/common/crafting/IntersectionIngredient
TYPE_RENAME	net/minecraftforge/common/util/TransformationHelper	net/neoforged/neoforge/common/util/TransformationHelper
TYPE_RENAME	net/minecraftforge/common/world/ClimateSettingsBuilder	net/neoforged/neoforge/common/world/ClimateSettingsBuilder
TYPE_RENAME	net/minecraftforge/eventbus/EventBus	net/neoforged/bus/EventBus
TYPE_RENAME	net/minecraftforge/fml/loading/FMLConfig$ConfigValue	net/neoforged/fml/loading/FMLConfig$ConfigValue
TYPE_RENAME	net/minecraftforge/registries/DataPackRegistriesHooks	net/neoforged/neoforge/registries/DataPackRegistriesHooks
TYPE_RENAME	net/minecraftforge/registries/holdersets/AndHolderSet	net/neoforged/neoforge/registries/holdersets/AndHolderSet
TYPE_RENAME	net/minecraftforge/client/ClientCommandSourceStack	net/neoforged/neoforge/client/ClientCommandSourceStack
TYPE_RENAME	net/minecraftforge/common/IMinecartCollisionHandler	net/neoforged/neoforge/common/IMinecartCollisionHandler
TYPE_RENAME	net/minecraftforge/common/WorldWorkerManager	net/neoforged/neoforge/common/WorldWorkerManager
TYPE_RENAME	net/minecraftforge/common/WorldWorkerManager$IWorker	net/neoforged/neoforge/common/WorldWorkerManager$IWorker
TYPE_RENAME	net/minecraftforge/common/world/ModifiableStructureInfo$StructureInfo$Builder	net/neoforged/neoforge/common/world/ModifiableStructureInfo$StructureInfo$Builder
TYPE_RENAME	net/minecraftforge/common/world/StructureModifier	net/neoforged/neoforge/common/world/StructureModifier
TYPE_RENAME	net/minecraftforge/common/world/StructureModifier$Phase	net/neoforged/neoforge/common/world/StructureModifier$Phase
TYPE_RENAME	net/minecraftforge/common/world/StructureSettingsBuilder	net/neoforged/neoforge/common/world/StructureSettingsBuilder
TYPE_RENAME	net/minecraftforge/eventbus/ListenerList	net/neoforged/bus/ListenerList
TYPE_RENAME	net/minecraftforge/fml/CrashReportCallables	net/neoforged/fml/CrashReportCallables
TYPE_RENAME	net/minecraftforge/fml/Logging	net/neoforged/fml/Logging
TYPE_RENAME	net/minecraftforge/fml/loading/ImmediateWindowHandler	net/neoforged/fml/loading/ImmediateWindowHandler
TYPE_RENAME	net/minecraftforge/fml/loading/StringUtils	net/neoforged/fml/loading/StringUtils
TYPE_RENAME	net/minecraftforge/fml/loading/VersionInfo	net/neoforged/fml/loading/VersionInfo
TYPE_RENAME	net/minecraftforge/fml/loading/moddiscovery/ModAnnotation$EnumHolder	net/neoforged/fml/loading/modscan/ModAnnotation$EnumHolder
TYPE_RENAME	net/minecraftforge/internal/BrandingControl	net/neoforged/neoforge/internal/BrandingControl
TYPE_RENAME	net/minecraftforge/registries/holdersets/NotHolderSet	net/neoforged/neoforge/registries/holdersets/NotHolderSet
TYPE_RENAME	net/minecraftforge/accesstransformer/Target	net/minecraft/world/level/pathfinder/Target
TYPE_RENAME	net/minecraftforge/client/CreativeModeTabSearchRegistry	net/neoforged/neoforge/client/CreativeModeTabSearchRegistry
TYPE_RENAME	net/minecraftforge/client/DimensionSpecialEffectsManager	net/neoforged/neoforge/client/DimensionSpecialEffectsManager
TYPE_RENAME	net/minecraftforge/client/NamedRenderTypeManager	net/neoforged/neoforge/client/NamedRenderTypeManager
TYPE_RENAME	net/minecraftforge/client/gui/ModListScreen	net/neoforged/neoforge/client/gui/ModListScreen
TYPE_RENAME	net/minecraftforge/client/loading/ClientModLoader	net/neoforged/neoforge/client/loading/ClientModLoader
TYPE_RENAME	net/minecraftforge/client/textures/UnitTextureAtlasSprite	net/neoforged/neoforge/client/textures/UnitTextureAtlasSprite
TYPE_RENAME	net/minecraftforge/common/IExtensibleEnum	net/neoforged/fml/common/asm/enumextension/IExtensibleEnum
TYPE_RENAME	net/minecraftforge/common/LenientUnboundedMapCodec	net/neoforged/neoforge/common/LenientUnboundedMapCodec
TYPE_RENAME	net/minecraftforge/common/SoundAction	net/neoforged/neoforge/common/SoundAction
TYPE_RENAME	net/minecraftforge/common/ticket/AABBTicket	net/neoforged/neoforge/common/ticket/AABBTicket
TYPE_RENAME	net/minecraftforge/common/util/FakePlayer$FakePlayerNetHandler	net/neoforged/neoforge/common/util/FakePlayer$FakePlayerNetHandler
TYPE_RENAME	net/minecraftforge/common/util/Size2i	net/neoforged/neoforge/common/util/Size2i
TYPE_RENAME	net/minecraftforge/common/util/TablePrinter	net/neoforged/neoforge/common/util/TablePrinter
TYPE_RENAME	net/minecraftforge/common/util/TransformationHelper$Deserializer	net/neoforged/neoforge/common/util/TransformationHelper$Deserializer
TYPE_RENAME	net/minecraftforge/common/world/BiomeSpecialEffectsBuilder	net/neoforged/neoforge/common/world/BiomeSpecialEffectsBuilder
TYPE_RENAME	net/minecraftforge/common/world/PieceBeardifierModifier	net/neoforged/neoforge/common/world/PieceBeardifierModifier
TYPE_RENAME	net/minecraftforge/common/world/StructureSettingsBuilder$StructureSpawnOverrideBuilder	net/neoforged/neoforge/common/world/StructureSettingsBuilder$StructureSpawnOverrideBuilder
TYPE_RENAME	net/minecraftforge/eventbus/BusBuilderImpl	net/neoforged/bus/BusBuilderImpl
TYPE_RENAME	net/minecraftforge/eventbus/EventBusErrorMessage	net/neoforged/bus/EventBusErrorMessage
TYPE_RENAME	net/minecraftforge/eventbus/api/BusBuilder	net/neoforged/bus/api/BusBuilder
TYPE_RENAME	net/minecraftforge/fml/ISystemReportExtender	net/neoforged/fml/ISystemReportExtender
TYPE_RENAME	net/minecraftforge/fml/ModLoadingException	net/neoforged/fml/ModLoadingException
TYPE_RENAME	net/minecraftforge/fml/ModWorkManager	net/neoforged/fml/ModWorkManager
TYPE_RENAME	net/minecraftforge/fml/config/ConfigTracker	net/neoforged/fml/config/ConfigTracker
TYPE_RENAME	net/minecraftforge/fml/javafmlmod/AutomaticEventSubscriber	net/neoforged/fml/javafmlmod/AutomaticEventSubscriber
TYPE_RENAME	net/minecraftforge/fml/loading/ImmediateWindowProvider	net/neoforged/neoforgespi/earlywindow/ImmediateWindowProvider
TYPE_RENAME	net/minecraftforge/fml/loading/TracingPrintStream	net/neoforged/fml/loading/TracingPrintStream
TYPE_RENAME	net/minecraftforge/fml/loading/moddiscovery/ModValidator	net/neoforged/fml/loading/moddiscovery/ModValidator
TYPE_RENAME	net/minecraftforge/fml/loading/progress/ProgressMeter	net/neoforged/fml/loading/progress/ProgressMeter
TYPE_RENAME	net/minecraftforge/registries/holdersets/AnyHolderSet	net/neoforged/neoforge/registries/holdersets/AnyHolderSet
TYPE_RENAME	net/minecraftforge/registries/holdersets/CompositeHolderSet	net/neoforged/neoforge/registries/holdersets/CompositeHolderSet
TYPE_RENAME	net/minecraftforge/registries/holdersets/HolderSetType	net/neoforged/neoforge/registries/holdersets/HolderSetType
TYPE_RENAME	net/minecraftforge/registries/holdersets/ICustomHolderSet	net/neoforged/neoforge/registries/holdersets/ICustomHolderSet

# EventBusSubscriber.Bus must be renamed -- the loader scans the annotation, so a shimmed enum
# would never be found -- and NeoForge dropped its bus() accessor. The accessor moves to a static.
METHOD_TO_STATIC	net/neoforged/fml/common/EventBusSubscriber$Bus	bus	()Ljava/util/function/Supplier;	easyport/bridge/BusBridge	bus	(Lnet/neoforged/fml/common/EventBusSubscriber$Bus;)Ljava/util/function/Supplier;
FIELD_TO_STATIC	net/neoforged/fml/common/EventBusSubscriber$Bus	FORGE	Lnet/neoforged/fml/common/EventBusSubscriber$Bus;	easyport/bridge/BusBridge	forge	()Lnet/neoforged/fml/common/EventBusSubscriber$Bus;

# --- Phase 4: types found by offline bytecode verification -------------------------------------
#
# These came from tools/verify-sweep.sh rather than from the type gap report, and the difference
# is worth knowing. A type a mod *extends or implements* barely registers in the gap report --
# it appears once per jar, not once per call -- but it fails the whole class at load. Verification
# ranks by what breaks rather than by how often it is mentioned, so it surfaces a different and
# more urgent set.
#
# Each target below was checked member by member before being written. ToolAction and ItemAbility
# agree on get/name/getActions; BaseFlowingFluid keeps ForgeFlowingFluid's protected
# Properties-taking constructor and the same builder; DeferredSpawnEggItem keeps the
# (Supplier,int,int,Item$Properties) constructor mods subclass.

# 21 jars. Forge's ToolAction and NeoForge's ItemAbility are the same three-method value type.
TYPE_RENAME	net/minecraftforge/common/ToolAction	net/neoforged/neoforge/common/ItemAbility
TYPE_RENAME	net/minecraftforge/common/ToolActions	net/neoforged/neoforge/common/ItemAbilities

# 36 jars, and mods subclass it, so the constructor mattered more than the name. It matches.
TYPE_RENAME	net/minecraftforge/common/ForgeSpawnEggItem	net/neoforged/neoforge/common/DeferredSpawnEggItem

# 14 jars. Renamed wholesale, nested Properties/Source/Flowing included and unchanged in shape.
TYPE_RENAME	net/minecraftforge/fluids/ForgeFlowingFluid	net/neoforged/neoforge/fluids/BaseFlowingFluid
TYPE_RENAME	net/minecraftforge/fluids/ForgeFlowingFluid$Properties	net/neoforged/neoforge/fluids/BaseFlowingFluid$Properties
TYPE_RENAME	net/minecraftforge/fluids/ForgeFlowingFluid$Source	net/neoforged/neoforge/fluids/BaseFlowingFluid$Source
TYPE_RENAME	net/minecraftforge/fluids/ForgeFlowingFluid$Flowing	net/neoforged/neoforge/fluids/BaseFlowingFluid$Flowing

# Vanilla dropped the "Dirt" variant when the screen stopped drawing a dirt background. Same
# single-Component constructor. This is what cyclopscore has been blocked on since Phase 3.
TYPE_RENAME	net/minecraft/client/gui/screens/GenericDirtMessageScreen	net/minecraft/client/gui/screens/GenericMessageScreen

# PositionImpl was the record-shaped implementation of Position; 1.21 deleted it and Vec3 already
# implemented the same interface with the same (DDD) constructor.
TYPE_RENAME	net/minecraft/core/PositionImpl	net/minecraft/world/phys/Vec3

# --- Relocate-then-rename: vanilla types 1.21 deleted ------------------------------------------
#
# A mod jar cannot supply a class under net.minecraft -- module resolution refuses it, proven in
# testkit/vanilla-package-probe -- so a deleted vanilla type is replaced by a stand-in under
# easyport.vanilla and every reference is rewritten to it.
#
# Read the stand-ins before trusting these. Most restore *loading* and not behaviour, because the
# vanilla code that used to call them is gone; DyeableLeatherItem is the exception, since dye
# colour was moved to a data component rather than removed and every method has an exact
# equivalent. The per-jar report is where that difference has to show up -- a clean load must not
# be read as a clean port.

# 34 jars. Advancement criteria moved to codecs in 1.20.5; the JSON-serialising base class and
# the predicate type in its constructor both went. Both are relocated, because a constructor is
# resolved by descriptor and the super(...) calls name the predicate type.
TYPE_RENAME	net/minecraft/advancements/critereon/AbstractCriterionTriggerInstance	easyport/vanilla/AbstractCriterionTriggerInstance
TYPE_RENAME	net/minecraft/advancements/critereon/ContextAwarePredicate	easyport/vanilla/ContextAwarePredicate

# 16 jars, and the one stand-in here that genuinely works: DYED_COLOR replaced the interface
# rather than removing the feature, so getColor and setColor read and write the real stack.
TYPE_RENAME	net/minecraft/world/item/DyeableLeatherItem	easyport/vanilla/DyeableLeatherItem

# --- COERCE: a vanilla type that stopped being assignable to what it replaced ------------------
#
# COERCE  from <TAB> to <TAB> bridgeOwner <TAB> bridgeName
#
# Inserts a conversion at the argument position where the old type meets a signature that now
# wants the new one, using the same spill-and-reload machinery as Holder wrapping -- structurally
# it is the same problem, a value needing conversion at the vanilla boundary.
#
# Unlike Holder wrapping this cannot be discovered from descriptors, because there is no family
# to recognise: each of these is a one-off shape change, and the only evidence that two types are
# related is knowing what happened between the versions.

# ModelResourceLocation extended ResourceLocation in 1.20.1 and became a record holding one in
# 1.21, so mods pass them interchangeably and every such call now fails verification. The largest
# single remaining cause in the sweep -- 4 of 22 mods.
COERCE	net/minecraft/client/resources/model/ModelResourceLocation	net/minecraft/resources/ResourceLocation	easyport/bridge/VanillaBridge	modelId

# Deleted vanilla types, batch two. Nested names flatten -- a relocated class cannot keep its
# outer class when the outer class is the thing that survived.
TYPE_RENAME	net/minecraft/world/item/RecordItem	easyport/vanilla/RecordItem
TYPE_RENAME	net/minecraft/world/item/BowlFoodItem	easyport/vanilla/BowlFoodItem
TYPE_RENAME	net/minecraft/data/recipes/FinishedRecipe	easyport/vanilla/FinishedRecipe
TYPE_RENAME	net/minecraft/core/particles/ParticleOptions$Deserializer	easyport/vanilla/ParticleOptionsDeserializer
TYPE_RENAME	net/minecraft/world/level/storage/loot/LootDataManager	easyport/vanilla/LootDataManager
TYPE_RENAME	net/minecraft/world/level/storage/loot/LootDataId	easyport/vanilla/LootDataId
TYPE_RENAME	net/minecraft/world/level/storage/loot/functions/LootItemConditionalFunction$Serializer	easyport/vanilla/LootItemConditionalFunctionSerializer
TYPE_RENAME	net/minecraft/world/level/storage/loot/entries/LootPoolSingletonContainer$Serializer	easyport/vanilla/LootPoolSingletonContainerSerializer

# The GUI overlay family. NeoForge replaced Forge's named-overlay registry with LayeredDraw
# layers keyed by ResourceLocation, so RenderGuiOverlayEvent cannot be renamed onto
# RenderGuiLayerEvent -- getOverlay() would return the wrong type. Shim plus GuiOverlayBridge,
# the same shape the tick and network events use. 3 of the 22 sampled mods.

# IContext is renamed and ICondition is shimmed, which looks inconsistent and is not. IContext
# kept its exact shape (EMPTY, getTag, getAllTags), so the rename is safe and *necessary* -- it is
# what makes a mod's test(IContext) satisfy NeoForge's abstract method. ICondition gained a second
# abstract method, codec(), which mods compiled against 1.20.1 cannot have implemented; renaming
# it would resolve cleanly and then throw AbstractMethodError on the first recipe that used one.
TYPE_RENAME	net/minecraftforge/common/crafting/conditions/ICondition$IContext	net/neoforged/neoforge/common/conditions/ICondition$IContext

# --- INTERFACE_SUBSTITUTE: a vanilla interface that became a record ----------------------------
#
# INTERFACE_SUBSTITUTE  platformType <TAB> substitute
#
# Applied only in the implements clause of a class that actually implements the type. Deliberately
# not a TYPE_RENAME: that would also rewrite the mod's reads of vanilla's own constants, which are
# real records rather than implementations of the substitute, and the mod would fail verification
# somewhere else entirely. Same trap the Holder work fell into by letting a new type spread.
#
# The paired COERCE converts an instance back into the real record wherever one is passed to
# vanilla, including from mod classes the substitution created -- which is why Translate collects
# substituted classes in a pass over the whole jar before rewriting any of them.

# 53 corpus jars reference ArmorMaterial, and defining a custom one by implementing the interface
# was the standard 1.20.1 idiom. 1.21 made it a record, so every such class fails to load outright
# and takes the items registered beside it. The conversion is total -- every component of the
# record has a getter on the old interface.
INTERFACE_SUBSTITUTE	net/minecraft/world/item/ArmorMaterial	easyport/vanilla/ArmorMaterial
COERCE	easyport/vanilla/ArmorMaterial	net/minecraft/world/item/ArmorMaterial	easyport/bridge/ArmorMaterialBridge	toVanilla

# Forge patched ItemStack to extend CapabilityProvider, so this compiled with a parameter type
# that no longer exists and an argument that was never really one. Neither a shim nor a rename
# reaches a patched vanilla method; the call moves to a bridge, and the second parameter becomes
# Object because the call site still names Forge's type.
METHOD_TO_STATIC	net/minecraft/world/item/ItemStack	areCapsCompatible	(Lnet/minecraftforge/common/capabilities/CapabilityProvider;)Z	easyport/bridge/CapabilityBridge	areCapsCompatible	(Lnet/minecraft/world/item/ItemStack;Ljava/lang/Object;)Z

# Ingredient is final in 1.21, so a mod's custom ingredient cannot be one. NeoForge's
# ICustomIngredient is deliberately not an Ingredient either -- it converts into one -- which is
# the same shape as the ArmorMaterial substitution and handled the same way.
COERCE	net/minecraftforge/common/crafting/AbstractIngredient	net/minecraft/world/item/crafting/Ingredient	easyport/bridge/IngredientBridge	toVanilla

# --- ARG_FILL: a parameter 1.21 added ----------------------------------------------------------
#
# ARG_FILL  parameterType <TAB> bridgeOwner <TAB> bridgeName
#
# A call site compiled against 1.20.1 cannot supply an argument that did not exist, so the value
# comes from a bridge and the transformer inserts it at whichever position explains the new
# signature. If two positions both explain it the call is left alone and reported -- inserting in
# the wrong one produces a call that links and passes every argument to the wrong parameter.
#
# HolderLookup.Provider is the heaviest of these by far: 1.20.5 added it to the serialization
# family when NBT became data components.
ARG_FILL	net/minecraft/core/HolderLookup$Provider	easyport/bridge/RegistryAccessBridge	provider
ARG_FILL	com/mojang/serialization/MapCodec	easyport/bridge/CodecBridge	particleCodec
ARG_FILL	net/minecraft/network/codec/StreamCodec	easyport/bridge/CodecBridge	particleStreamCodec

# ParticleType(boolean, Deserializer) became (boolean, MapCodec, StreamCodec). One argument
# changed type and one was added, so this needs both a COERCE and an ARG_FILL -- which is the
# point of having them as separate mechanisms that compose.
COERCE	easyport/vanilla/ParticleOptionsDeserializer	com/mojang/serialization/MapCodec	easyport/bridge/CodecBridge	particleCodecFrom

# DropExperienceBlock lost its one-argument constructor; the surviving form takes an IntProvider.
# Constant zero is exactly what the old one meant, so nothing is lost. Blocks alltheores.
ARG_FILL	net/minecraft/util/valueproviders/IntProvider	easyport/bridge/VanillaBridge	noExperience

# AttributeModifier.Operation constants renamed, same meanings. A field read cannot be renamed by
# the type remapper -- that rewrites owners, not member names -- so each becomes a bridge call.
FIELD_TO_STATIC	net/minecraft/world/entity/ai/attributes/AttributeModifier$Operation	ADDITION	Lnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;	easyport/bridge/VanillaBridge	addValue	()Lnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;
FIELD_TO_STATIC	net/minecraft/world/entity/ai/attributes/AttributeModifier$Operation	MULTIPLY_BASE	Lnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;	easyport/bridge/VanillaBridge	multiplyBase	()Lnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;
FIELD_TO_STATIC	net/minecraft/world/entity/ai/attributes/AttributeModifier$Operation	MULTIPLY_TOTAL	Lnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;	easyport/bridge/VanillaBridge	multiplyTotal	()Lnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;

# AttributeModifier(UUID, String, double, Operation) became (ResourceLocation, double, Operation):
# the id and the display name collapsed into one identifier. Two parameters becoming one is past
# what ARG_DROP or a coercion can express, so the construction moves to a factory. 193 jar-weight.
CTOR_TO_STATIC	net/minecraft/world/entity/ai/attributes/AttributeModifier	(Ljava/util/UUID;Ljava/lang/String;DLnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;)V	attributeModifier	(Ljava/util/UUID;Ljava/lang/String;DLnet/minecraft/world/entity/ai/attributes/AttributeModifier$Operation;)Lnet/minecraft/world/entity/ai/attributes/AttributeModifier;	easyport/bridge/VanillaBridge
